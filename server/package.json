{
	"name": "chatvat-server",
	"version": "1.0.0",
	"description": "ChatVat Chat Application server",
	"main": "server.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "nodemon server.js"
	},
	"author": "Prince Jagani",
	"license": "ISC",
	"dependencies": {
		"nodemon": "^3.0.2"
	}
}
